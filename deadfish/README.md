This is an local DeadFish esolang intepreter programmed in C++, it features the 4 commnads in the real language and the exact same overflow error check.
To run copy deadfish.h then in your project

#include "deadfish.h'

...

int main() {
DeadFish f;
f.runcode("iisso"); // Basic code example
}
